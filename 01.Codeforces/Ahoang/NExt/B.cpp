#include<bits/stdc++.h>

#define ll long long
#define X first
#define Y second

#define vi vector<ll>
#define ii pair<ll,ll>
#define vii vector<ii>

const long long MAX = 3e3 + 5;
const long long mod = 1e9 + 7;
const long long INF = 1e9;

using namespace std;

ll n;
ll a[MAX];
ll dp[MAX][MAX];

ll calc(ll l,ll r,bool ok){
	
	 
	
	for(ll i = l + 1;i < r;i++){
		
	}
}

signed main(){
	cin>>n;
	for(ll i = 1;i <= n;i++)cin>>a[i];
	calc(1,n,0);
	
}